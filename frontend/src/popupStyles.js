const popupContent = {
    textAlign: "center",
    marginTop: "10px"
  };
  const popupHead = {
    fontWeight: "bold",
    fontSize: "22px"
  };
  const popupText = {
    fontSize: "15px",
    marginBottom: "20px"
  };
  const popupBox = {
    borderRadius: "0px",
    };
  
  export { popupContent, popupHead, popupText, popupBox };
  